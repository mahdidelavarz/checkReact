const address = '../../Assets/Images/';

const logo = require(`${address}logo.png`);
const laam = require(`${address}laam.png`);
const kiseh = require(`${address}kiseh.png`);
const dastkesh = require(`${address}dastkesh.png`);
const pad = require(`${address}pad.png`);



const log = require(`${address}log.png`);
const logo_long = require(`${address}Logo-green-long.png`);
const about = require(`${address}about.jpg`);
const happy_boy = require(`${address}happy_boy.png`);
const auto_back = require(`${address}auth_back.jpg`);
const auto_back_rtl = require(`${address}auth_back_rtl.jpg`);
const analytsis_history = require(`${address}analytsis_history.png`);
const specia_list = require(`${address}specia_list.png`);
const sperm_analysis = require(`${address}sperm_analysis.png`);
const help = require(`${address}help.png`);
const gadget = require(`${address}gadget.png`);
const clip = require(`${address}clip.png`);
const profile_back = require(`${address}profile_back.png`);
const alert = require(`${address}alert.png`);
const help_circle = require(`${address}help_circle.png`);
const emptyHistory = require(`${address}emptyHistory.png`);
const gadgetScan = require(`${address}gadgetScan.png`);
const attachGadget = require(`${address}attachGadget.png`);
const coloringSample = require(`${address}coloringSample.png`);
const prepareSlide = require(`${address}prepareSlide.png`);
const recVideo = require(`${address}recVideo.png`);
const loading = require(`${address}loading.gif`);
const CupFill = require(`${address}CupFill.gif`);
const CupSorang = require(`${address}CupSorang.gif`);
const LaamFinal = require(`${address}LaamFinal.gif`);

const videoSuccess = require(`${address}videoSuccess.png`);
const iran = require(`${address}iran.png`);
const english = require(`${address}english.png`);
const support_back = require(`${address}support_back.png`);
const inviteBack = require(`${address}inviteBack.png`);
const empty_image = require(`${address}empty_image.png`);
const process = require(`${address}process.png`);
const syringe = require(`${address}syringe.png`);
const health_education = require(`${address}health_education.png`);
const text_education = require(`${address}text_education.png`);

const test1 = require(`${address}test1.jpg`);
const test2 = require(`${address}test2.jpg`);

const ic_change_lang = require(`${address}ic_change_lang.png`);
const ic_circle = require(`${address}ic_circle.png`);
const ic_circle_check = require(`${address}ic_circle_check.png`);
const ic_off_eye = require(`${address}ic_off_eye.png`);
const ic_eye = require(`${address}ic_eye.png`);
const ic_user = require(`${address}ic_user.png`);
const ic_home = require(`${address}ic_home.png`);
const ic_back = require(`${address}ic_back.png`);
const ic_left_back = require(`${address}ic_left_back.png`);
const ic_arrow = require(`${address}ic_arrow.png`);
const ic_country = require(`${address}ic_country.png`);
const ic_filter = require(`${address}ic_filter.png`);
const ic_search = require(`${address}ic_search.png`);
const ic_close = require(`${address}ic_close.png`);
const ic_location = require(`${address}ic_location.png`);
const ic_description = require(`${address}ic_description.png`);
const ic_phone = require(`${address}ic_phone.png`);
const ic_help = require(`${address}ic_help.png`);
const ic_cup = require(`${address}ic_cup.png`);
const ic_slide = require(`${address}ic_slide.png`);
const ic_pipette = require(`${address}ic_pipette.png`);
const ic_date = require(`${address}ic_date.png`);
const ic_about = require(`${address}ic_about.png`);
const ic_passcode = require(`${address}ic_passcode.png`);
const ic_scan = require(`${address}ic_scan.png`);
const ic_recording_video = require(`${address}ic_recording_video.png`);
const ic_ok = require(`${address}ic_ok.png`);
const ic_refresh = require(`${address}ic_refresh.png`);
const ic_credit = require(`${address}ic_credit.png`);
const ic_exit_app = require(`${address}ic_exit_app.png`);
const ic_discount = require(`${address}ic_discount.png`);
const ic_inviteFriend = require(`${address}ic_inviteFriend.png`);
const ic_update = require(`${address}ic_update.png`);
const ic_share = require(`${address}ic_share.png`);
const ic_support = require(`${address}ic_support.png`);
const ic_analysis = require(`${address}ic_analysis.png`);
const ic_gmail = require(`${address}ic_gmail.png`);
const ic_mobile = require(`${address}ic_mobile.png`);
const ic_barcode = require(`${address}ic_barcode.png`);
const ic_direction = require(`${address}ic_direction.png`);

export {
    log,
    logo,
    logo_long,
    about,
    happy_boy,
    auto_back,
    auto_back_rtl,
    analytsis_history,
    specia_list,
    sperm_analysis,
    help,
    gadget,
    clip,
    profile_back,
    alert,
    help_circle,
    emptyHistory,
    gadgetScan,
    attachGadget,
    coloringSample,
    prepareSlide,
    recVideo,
    loading,
    LaamFinal,
    CupSorang,
    CupFill,
    videoSuccess,
    iran,
    english,
    support_back,
    inviteBack,
    empty_image,
    process,
    syringe,
    health_education,
    text_education,
    laam,
    kiseh,
    dastkesh,
    pad,

    test1,
    test2,

    ic_change_lang,
    ic_circle,
    ic_circle_check,
    ic_off_eye,
    ic_eye,
    ic_user,
    ic_home,
    ic_back,
    ic_left_back,
    ic_arrow,
    ic_country,
    ic_filter,
    ic_search,
    ic_close,
    ic_location,
    ic_description,
    ic_phone,
    ic_help,
    ic_cup,
    ic_slide,
    ic_pipette,
    ic_date,
    ic_about,
    ic_passcode,
    ic_scan,
    ic_barcode,
    ic_recording_video,
    ic_ok,
    ic_refresh,
    ic_credit,
    ic_exit_app,
    ic_discount,
    ic_inviteFriend,
    ic_update,
    ic_share,
    ic_support,
    ic_analysis,
    ic_gmail,
    ic_mobile,
    ic_direction
};